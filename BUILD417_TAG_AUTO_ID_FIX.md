# BUILD417 タグ自動採番修正

## 不具合
タグは `data.tags` に保存される一方、自動採番処理が `data.masters.tags` のみを参照していたため、常に `TAG-0001` を生成して既存ID重複となっていました。

## 修正
- タグ分類では `tagDefinitions()`（`data.tags`）を採番対象に使用
- その他のマスター分類では従来どおり `data.masters[category]` を使用
- 既存の最大連番に1を加え、`TAG-0001` 形式で生成

## 期待動作
- タグが0件: `TAG-0001`
- `TAG-0001` 登録済み: `TAG-0002`
- `TAG-0001`, `TAG-0005` 登録済み: `TAG-0006`
