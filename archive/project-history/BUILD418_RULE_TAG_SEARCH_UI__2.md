# BUILD418 ルールタグ検索UI

- 通常タグの全件プルダウンを検索コンボボックスへ変更
- タグ名、ID、カテゴリ、親タグ、説明を部分一致検索
- 候補は最大50件
- 空欄時は最近使用したタグを優先表示
- 配置先名称に一致する候補を優先順位へ反映
- 上下キー、Enter、Escに対応
- 数値タグの親タグ選択も、現在のルール内に配置済みの通常タグを対象とする検索UIへ変更
- 削除済みまたは無効タグは通常タグ候補から除外
- BUILD表示をBUILD418、APP_VERSIONを1.25.0-devへ更新
