# BUILD423 Unified Mobile Layout Fix

## 修正内容
- 横画面要求オーバーレイを削除・恒久無効化
- 900px以下では1600×900固定キャンバスを解除し、通常のレスポンシブ表示へ統一
- 縦画面でタイトル、拠点、イベント、戦闘、結果を1カラム化
- 戦闘ユニットを縦画面でも常時表示
- SafariのvisualViewport変化と画面回転後にモバイル状態を再同期
- 縦画面起動時の開発ワークスペース自動遷移を停止
- Studioリンクを配置階層に応じた相対URLへ変更

## 対象
- `/index.html`
- `/formal-v03/index.html`

## 注意
デスクトップの固定1600×900表示は維持し、900px以下のみレスポンシブ表示へ切り替えます。
