# BUILD429 Duplicate authority map

BUILD428で見つかった完全一致ファイルを、削除せずに「正式データ元」と「生成・検証用コピー」に分類した記録です。

## CPF

- 正式データ元: `/cpf/`
- 出力・配布確認用コピー: `/Export/cpf/`
- 方針: 現段階では両方を残す。編集は原則 `/cpf/` 側で行い、Export生成時に同期する。

## Export内の空JSON

同じ内容でも、分類ごとに必要なプレースホルダーです。重複削除の対象にしません。

## tests/fixtures

旧版互換テスト用の固定入力です。現行データと同一でも削除しません。

## 判定

- 今回削除可能と確定した実行ファイル: 0
- 今回削除したファイル: 0
- 物理移動した実行ファイル: 0
