# Runtime Test Provenance Recheck — GA-B486.115 / GKS-B527

## Purpose

Reclassify runtime tests after the R04 handoff boundary so a PASS result cannot be mistaken for current Studio Export acceptance.

## R04 recheck

- `tests/*r04*.js`: 22 files, all re-run against the current source tree and PASS.
- R04 device entrypoints execute through `game/index.html` production runtime, but the Generic skills are generated as `device_fixture` data at test time.
- Therefore R04 device results prove production-runtime connection for the Generic contracts under test. They do **not** by themselves prove that the current Studio Export contains and executes equivalent definitions.
- Saved repository evidence exists under `docs/validation/generic-runtime-acceptance-GA-B486.104/`; it remains historical acceptance evidence and is not rewritten.

## R05 recheck

- Existing R05 A-H Node tests are contract/runtime fixtures. No R05 real-device JSON entrypoint existed at GA-B486.114.
- GA-B486.115 adds one consolidated real-device entrypoint: `R05-GENERIC-DIRECT-RUNTIME-DEVICE-001`.
- It covers DAMAGE, HEAL, APPLY, REMOVE, MP RESOURCE_CHANGE, REVIVE, TARGET_CONTROL(COVER), and SPECIAL boundary.
- Its JSON provenance explicitly states:
  - generated from the current Generic contract at test time,
  - Studio Export is not used,
  - legacy/demo Export is not used,
  - Studio Export acceptance is a separate gate.

## R06 recheck

- R06-A and R06-B remain fixed-fixture migration/comparison tests.
- R06-B is therefore reopened for test provenance / real-device acceptance and is not considered current-data acceptance complete.

## Formal runtime Export freshness

The currently bundled `Export/skill/skills.json` is still identified by the runtime configuration as `skills.json?v=48658`; the previously generated formal regression report identifies its data as GA-B486.58 and generated by GKS-B484.

This Export may still be useful as historical regression input, but it must not be labeled as current GKS-B527 Studio Export acceptance.

## Acceptance layers after this change

1. **Internal contract regression** — Node fixtures (R03/R04/R05/R06).
2. **Production runtime / real-device contract acceptance** — R04 device fixtures plus new consolidated R05 device fixture test.
3. **Current Studio Export acceptance** — separate, still pending until a current Studio-generated Skill Export is available and freshness can be proven.
