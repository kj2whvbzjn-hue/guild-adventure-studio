# タグスキル実装 Sprint 1

作成日: 2026-08-05

## 実装済み

- 現行 `game/` を変更せず、`game-tag-test/` を新設
- 一般タグと数値タグの解析
- タグの静的検証
- Runtime Skill Definitionへの正規化
- `ATTACK` ロジックの実行
- `DAMAGE=100` を攻撃力100%として既存HP・演出・ログへ接続
- 行動予約へ `skillId` を保持
- 予約対象を固定し、対象無効時は再選択せず失敗
- 開発者画面へ手動コンパイル・手動実行UIを追加
- 不正スキルをゲーム停止させずスキル単位で拒否
- DOTタグの解析と必須値検証（実行はSprint 2へ保留）

## テストスキル

1. タグ攻撃テスト: `ATTACK / 敵 / 単体 / 物理 / DAMAGE=100`
2. 強打テスト: `DAMAGE=150`
3. 不正攻撃: `DAMAGE`欠落
4. 毒斬り: `ATTACK + DOT`（DOT実行は未接続警告）

## 起動

静的HTTPサーバーでリポジトリルートを公開し、次を開きます。

`game-tag-test/`

開発者モードをONにすると「タグスキル Sprint 1 検証」パネルが表示されます。

## 自動検査

- JavaScript構文検査: PASS
- タグコンパイラ7項目: PASS
- 必須実装マーカー検査: PASS

## 次の作業

Sprint 2:

- DOTスタックデータを戦闘ユニットへ追加
- `DOT_INTERVAL`ごとのTick処理
- 個別スタックの持続時間管理
- ATTACK命中成功後のみDOT付与
- 最大スタック種別との接続
- 毒斬りの複合動作試験
