const fs=require('fs');const path=require('path');const root=path.resolve(__dirname,'..');const html=fs.readFileSync(path.join(root,'game','index.html'),'utf8');const sw=fs.readFileSync(path.join(root,'game','sw.js'),'utf8');const exp=JSON.parse(fs.readFileSync(path.join(root,'Export','skill','skills.json'),'utf8'));const rows=exp.data||[];const ids=new Set(rows.map(x=>x.id));const checks=[['build',html.includes('GA-B467 — Studio Export Tag Skill Bridge')],['button',html.includes('tagTestRunStudioBridge')],['loader',html.includes('function loadStudioSkillDefinitions')],['export URL',html.includes('../Export/skill/skills.json?v=467')],['validation id',html.includes('TAG-STUDIO-EXPORT-BRIDGE-001')],['attack export',ids.has('SKL-TEST-ATTACK')],['poison export',ids.has('SKL-TEST-POISON')],['buff export',ids.has('SKL-TEST-BUFF-10')],['debuff export',ids.has('SKL-TEST-DEBUFF-10')],['follow export',ids.has('SKL-TEST-FOLLOW-POISON')],['all tag arrays',rows.filter(x=>ids.has(x.id)&&x.id.startsWith('SKL-TEST-')).every(x=>Array.isArray(x.tags)&&x.tags.length)],['sw',sw.includes('guild-adventure-ga-b467')&&sw.includes('appv=467')&&sw.includes('../Export/skill/skills.json?v=467')]];let failed=0;for(const [n,ok] of checks){console.log(`${ok?'PASS':'FAIL'} ${n}`);if(!ok)failed++;}if(failed)process.exit(1);