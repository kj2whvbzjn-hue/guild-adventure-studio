const fs=require('fs'),assert=require('assert');
const app=fs.readFileSync('game/assets/js/app-runtime.js','utf8');
const ui=fs.readFileSync('game/assets/js/ui-bootstrap.js','utf8');
const shell=fs.readFileSync('assets/shared/js/game-shell-common.js','utf8');
assert(!/for\(const q of adventureQuestCatalog\).*QUESTS/.test(app),'formal Story quests must not be merged into legacy fixed-battle QUESTS');
assert(app.includes("function selectedQuest(){const formal=formalAdventureQuests();"),'selectedQuest must prefer formal Story quest catalog');
assert(ui.includes("await beginSelectedAdventure()"),'desktop bootstrap must route departure to QuestRun');
assert(!ui.includes("prepareEvent();setPhase('event')"),'desktop bootstrap must not overwrite formal departure with legacy event route');
assert(shell.includes("if(typeof beginSelectedAdventure==='function') beginSelectedAdventure();"),'mobile shell must route departure to QuestRun');
assert(!shell.includes("if(typeof prepareEvent==='function') prepareEvent();"),'mobile shell must not route departure to legacy event route');
assert(app.includes("$('eventScout'),$('eventSearch')"),'Adventure Playback must hide dynamically inserted legacy scout/search actions');
assert(app.includes("formalAdventureQuests().find(x=>String(x.id)===String(run?.quest_id))"),'Playback title must resolve formal quest without legacy QUESTS');
console.log('adventure-formal-departure-playback-routing-ga-b486-183-gks-b591 PASS');
