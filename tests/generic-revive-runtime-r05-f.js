const assert=require('assert'),fs=require('fs'),vm=require('vm');
const registry=JSON.parse(fs.readFileSync('assets/shared/config/skill-generic-registry.json','utf8')),generic=require('../assets/shared/js/generic-skill-compiler.js');assert.strictEqual(generic.VERSION,'R05-H');
for(const path of ['game/assets/js/tag-skill-runtime.js','game-tag-test/assets/js/tag-skill-runtime.js']){const events=[],ctx={console,battle:{tick:0,units:[],log:[]},recordValidationEvent:(type,payload)=>events.push({type,payload})};vm.createContext(ctx);vm.runInContext(fs.readFileSync(path,'utf8'),ctx);
 for(const [effect,expected] of [[{type:'REVIVE',hp:35},35],[{type:'REVIVE',hpRate:0.25},50]]){const skill={schemaVersion:1,id:`R05F-${expected}`,name:'Revive',trigger:{type:'ON_USE',scope:'SELF'},target:{side:'ALLY',range:'SINGLE'},effects:[effect],resource:{mpCost:0,cooldown:0}},out=generic.compileGenericSkill(skill,registry,ctx.compileTaggedSkill);assert.strictEqual(out.ok,true,JSON.stringify(out.errors));const tampered=JSON.parse(JSON.stringify(out.legacySkill));tampered.tags=tampered.tags.map(x=>x.startsWith('REVIVE_HP')?x.replace(/=.*/, '=1'):x);const compiled=ctx.compileTaggedSkill(tampered);assert.strictEqual(compiled.ok,true);const actor={id:'S',name:'Source',side:'ally',alive:true},target={id:'T',name:'Target',side:'ally',alive:false,hp:0,maxHp:200};const result=ctx.executeGenericReviveRuntime(actor,target,compiled);assert.strictEqual(result.ok,true);assert.strictEqual(target.hp,expected);}
 assert.ok(events.some(x=>x.type==='generic_revive_executed'));
}
console.log('GENERIC_REVIVE_RUNTIME_R05_F_PASS');
