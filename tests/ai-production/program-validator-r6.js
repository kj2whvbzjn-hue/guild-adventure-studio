#!/usr/bin/env node
'use strict';
const assert=require('assert');
const fs=require('fs');
const path=require('path');
const Validator=require('../../shared/ai/ai-program-validator.js');
const root=path.resolve(__dirname,'../..');
const ports=(outputs)=>({inputs:[{id:'in',kind:'flow',data_type:'flow'}],outputs:outputs.map((id)=>({id,kind:'flow',data_type:'flow'}))});
const dv='2.0.0';
const project={tags:[{id:'TAG-HP',name:'HP'}],masters:{skills:[{id:'SKL-HEAL',name:'回復',runtimeContracts:{targetContract:{side:'SELF',range:'SINGLE'}}}],ai_searches:[],ai_conditions:[{id:'AIC-HP',name:'HP条件',status:'active',data_version:dv,evaluator:'condition.hp',supported_subject_kind:['SELF'],ports:ports(['true','false']),parameter_schema:{type:'object',required:['threshold','tag_id'],properties:{threshold:{type:'number',minimum:0,maximum:1},tag_id:{type:'string',ref_kind:'tag'}},additionalProperties:false}}],ai_target_selectors:[],ai_actions:[{id:'AIA-HEAL',name:'回復',status:'active',data_version:dv,evaluator:'action.skill',ports:ports([]),parameter_schema:{type:'object',required:['skill_id'],properties:{skill_id:{type:'string',ref_kind:'skill'}},additionalProperties:false}},{id:'AIA-WAIT',name:'待機',status:'active',data_version:dv,evaluator:'action.wait',ports:ports([]),parameter_schema:{type:'object',properties:{},required:[],additionalProperties:false}}]}};
const predicate={logic:'ALL',clauses:[{predicate_master_id:'AIC-HP',params:{threshold:.5,tag_id:'TAG-HP'},negate:false}]};
const valid={schema_version:'2.0.0',data_version:dv,id:'AIP-VALID',name:'Valid',version:1,status:'valid',entry_node_id:'AIN-0001',nodes:[{instance_id:'AIN-0001',master_node_id:'AIC-HP',master_data_version:dv,node_type:'condition',position:{x:0,y:0},parameters:{subject_scope:'SELF',predicate}},{instance_id:'AIN-0002',master_node_id:'AIA-HEAL',master_data_version:dv,node_type:'action',position:{x:1,y:0},parameters:{skill_id:'SKL-HEAL'}},{instance_id:'AIN-0003',master_node_id:'AIA-WAIT',master_data_version:dv,node_type:'action',position:{x:1,y:1},parameters:{}}],edges:[{edge_id:'AIE-0001',from:{node_id:'AIN-0001',port_id:'true'},transition_kind:'NODE',to:{node_id:'AIN-0002',port_id:'in'}},{edge_id:'AIE-0002',from:{node_id:'AIN-0001',port_id:'false'},transition_kind:'NODE',to:{node_id:'AIN-0003',port_id:'in'}}],subroutines:[]};
const ok=Validator.validate(valid,project);assert.strictEqual(ok.valid,true,JSON.stringify(ok.issues));assert.deepStrictEqual(ok.summary,{ERROR:0,WARNING:0,INFO:0});
const incomplete=structuredClone(valid);incomplete.edges=incomplete.edges.filter((edge)=>edge.from.port_id!=='false');const incompleteResult=Validator.validate(incomplete,project);assert.strictEqual(incompleteResult.valid,false);assert(incompleteResult.issues.some((row)=>row.code==='AI_OUTPUT_REQUIRED'&&row.node_id==='AIN-0001'));assert(incompleteResult.issues.some((row)=>row.code==='AI_NODE_UNREACHABLE'&&row.node_id==='AIN-0003'&&row.severity==='WARNING'));assert(!incompleteResult.issues.some((row)=>row.code==='AI_INPUT_REQUIRED'&&row.node_id==='AIN-0003'));
const invalid=structuredClone(valid);invalid.entry_node_id='MISSING';invalid.nodes[0].parameters.predicate.clauses[0].params={threshold:2,tag_id:'MISSING'};invalid.nodes[0].master_data_version='1.0.0';invalid.nodes[1].master_node_id='AIA-MISSING';invalid.subroutines=[{id:'SUB-A',entry_node_id:'MISSING'}];
const bad=Validator.validate(invalid,project);assert.strictEqual(bad.valid,false);for(const code of ['AI_ENTRY_NOT_FOUND','AI_PREDICATE_PARAMETER_INVALID','AI_MASTER_VERSION_STALE','AI_MASTER_NOT_FOUND','AI_SUBROUTINE_ENTRY_MISSING'])assert(bad.issues.some((row)=>row.code===code),code);
const actionOutgoing=structuredClone(valid);actionOutgoing.edges.push({edge_id:'AIE-0003',from:{node_id:'AIN-0002',port_id:'next'},transition_kind:'NODE',to:{node_id:'AIN-0003',port_id:'in'}});const outgoingResult=Validator.validate(actionOutgoing,project);assert(outgoingResult.issues.some((row)=>row.code==='AI_ACTION_OUTGOING_EDGE'));assert(outgoingResult.issues.some((row)=>row.code==='AI_OUTPUT_PORT_INVALID'));
const converged=structuredClone(valid);converged.edges[0].to={node_id:'AIN-0003',port_id:'in'};const convergeResult=Validator.validate(converged,project);assert.strictEqual(convergeResult.valid,true,'multiple incoming transitions may converge on one input');assert(!convergeResult.issues.some((row)=>row.code==='AI_INPUT_AMBIGUOUS'));
const outputAmbiguous=structuredClone(valid);outputAmbiguous.edges.push({edge_id:'AIE-0003',from:{node_id:'AIN-0001',port_id:'true'},transition_kind:'NODE',to:{node_id:'AIN-0003',port_id:'in'}});const ambiguous=Validator.validate(outputAmbiguous,project);assert(ambiguous.issues.some((row)=>row.code==='AI_OUTPUT_AMBIGUOUS'));assert(!ambiguous.issues.some((row)=>row.code==='AI_INPUT_AMBIGUOUS'));
const unreachable=structuredClone(valid);unreachable.nodes.push({instance_id:'AIN-0004',master_node_id:'AIA-WAIT',master_data_version:dv,node_type:'action',position:{x:9,y:9},parameters:{}});const unreachableResult=Validator.validate(unreachable,project);assert.strictEqual(unreachableResult.valid,true,'unreachable spare chip must remain saveable');assert(unreachableResult.issues.some((row)=>row.code==='AI_NODE_UNREACHABLE'&&row.severity==='WARNING'));assert(!unreachableResult.issues.some((row)=>row.code==='AI_INPUT_REQUIRED'&&row.node_id==='AIN-0004'));
const cycle=structuredClone(valid);cycle.nodes[2]={instance_id:'AIN-0003',master_node_id:'AIC-HP',master_data_version:dv,node_type:'condition',position:{x:1,y:1},parameters:{subject_scope:'SELF',predicate:structuredClone(predicate)}};cycle.edges=cycle.edges.filter((edge)=>edge.to.node_id!=='AIN-0003');cycle.edges.push({edge_id:'AIE-0002',from:{node_id:'AIN-0001',port_id:'false'},transition_kind:'NODE',to:{node_id:'AIN-0003',port_id:'in'}},{edge_id:'AIE-0003',from:{node_id:'AIN-0003',port_id:'true'},transition_kind:'NODE',to:{node_id:'AIN-0001',port_id:'in'}},{edge_id:'AIE-0004',from:{node_id:'AIN-0003',port_id:'false'},transition_kind:'NODE',to:{node_id:'AIN-0002',port_id:'in'}});assert(Validator.validate(cycle,project).issues.some((row)=>row.code==='AI_CYCLE_UNBOUNDED'));
assert.deepStrictEqual(Validator.validate(invalid,project).issues,bad.issues,'issue order must be deterministic');
const manifest=JSON.parse(fs.readFileSync(path.join(root,'studio/ai-production/manifest.json'),'utf8'));const html=fs.readFileSync(path.join(root,'studio/index.html'),'utf8');assert.strictEqual(manifest.shared_canonical.program_validator,'shared/ai/ai-program-validator.js');assert(html.includes('../shared/ai/ai-program-validator.js?v=2'));assert(html.includes('GKSAIProgramValidator.validate(program,data)'));
console.log('AI_PROGRAM_VALIDATOR_R6_OK v2=1 predicate=1 exact_ports=1 mandatory_connections=1 action_terminal=1 multi_input=1 unreachable_saveable=1 cycle=1 deterministic=1');
