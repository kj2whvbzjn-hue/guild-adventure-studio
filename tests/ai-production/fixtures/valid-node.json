{
  "id": "AIC-HP-BELOW",
  "name": "HP threshold",
  "node_type": "condition",
  "status": "active",
  "data_version": "1.0.0",
  "description": "Checks whether the actor HP ratio is below the configured threshold.",
  "tags": ["hp", "condition"],
  "evaluator": "condition.hp_below",
  "ports": {
    "inputs": [{"id": "in", "kind": "flow", "data_type": "flow"}],
    "outputs": [
      {"id": "true", "kind": "flow", "data_type": "flow"},
      {"id": "false", "kind": "flow", "data_type": "flow"}
    ]
  },
  "parameter_schema": {
    "type": "object",
    "required": ["threshold"],
    "properties": {"threshold": {"type": "number", "minimum": 0, "maximum": 1}},
    "additionalProperties": false
  }
}
