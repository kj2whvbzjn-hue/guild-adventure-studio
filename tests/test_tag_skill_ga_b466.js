const fs=require('fs');const path=require('path');const root=path.resolve(__dirname,'..');const html=fs.readFileSync(path.join(root,'game','index.html'),'utf8');const sw=fs.readFileSync(path.join(root,'game','sw.js'),'utf8');const checks=[['build',html.includes('GA-B466 — Conditional Follow-up Runtime')],['button',html.includes('tagTestRunConditionalFollowUp')],['skill',html.includes('SKL-TEST-FOLLOW-POISON')],['trigger',html.includes('TRIGGER_ALLY_ATTACK')],['condition',html.includes('CONDITION_POISONED')],['dispatcher',html.includes('function dispatchConditionalFollowUps')],['loop guard',html.includes('FOLLOW_UP_CANNOT_CHAIN')],['test id',html.includes('TAG-FOLLOW-UP-POISONED-001')],['schema',html.includes("schema_version:'1.3.0'")],['sw',sw.includes('guild-adventure-ga-b466')&&sw.includes('appv=466')]];let failed=0;for(const [n,ok] of checks){console.log(`${ok?'PASS':'FAIL'} ${n}`);if(!ok)failed++;}if(failed)process.exit(1);