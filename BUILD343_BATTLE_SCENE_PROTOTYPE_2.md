# Build 343 — Battle Scene Prototype 2

## 採用レイアウト

- スマートフォン横持ちを表示基準とする。
- 左右対面方式を採用する。
- 標準（日本式）は「敵：左／味方：右」。
- 設定により「味方：左／敵：右」へ左右反転できる。
- 上下対面方式は採用しない。

## 実装

- 横画面で戦闘演出領域を動的ビューポート高まで拡張。
- 縦画面では横向き案内を表示。
- iOS等のセーフエリアを考慮。
- 左右反転設定をブラウザへ保存。
- 接近方向、攻撃方向、被弾ノックバック方向を配置設定に追従。
- Battle Coreは変更せず、Battle Scene側のみで座標を変換。

## 仕様上の注意

本Buildは演出基盤のプロトタイプであり、正式キャラクター素材、背景、最終的な隊形・UIではない。正式仕様にないジョブ固有スキルは含まない。
