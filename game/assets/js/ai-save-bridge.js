(function(root,factory){
  const Program=typeof module==='object'&&module.exports?require('../../../shared/ai/ai-program-model.js'):root?.GKSAIProgramModel;
  const Layout=typeof module==='object'&&module.exports?require('../../../shared/ai/ai-layout-model.js'):root?.GKSAILayoutModel;
  const api=factory(Program,Layout);
  if(typeof module==='object'&&module.exports)module.exports=api;
  if(root)root.GKGameAISaveBridge=api;
})(typeof globalThis!=='undefined'?globalThis:this,function(Program,Layout){
  'use strict';
  if(!Program||!Layout)throw new Error('Formal AI save dependencies are required');
  const SAVE_VERSION=4;
  const PRESET_ID_PATTERN=/^AIPR-([0-9]+)$/;
  const PLAYER_PROGRAM_ID_PATTERN=/^AIP-([0-9]+)$/;
  const PLAYER_LAYOUT_ID_PATTERN=/^AIL-([0-9]+)$/;
  const SAVE_KEYS=new Set(['saveVersion','schemaRevision','gameVersion','createdAt','updatedAt','characters','aiPrograms','aiLayouts','aiPresets','partyIds','selectedQuestId','inventory','guild','flags','quest_progress','quest_resources','adventure','gameSettings','tutorialProgress']);
  const SAVE_REQUIRED=Object.freeze(['saveVersion','schemaRevision','gameVersion','createdAt','updatedAt','characters','aiPrograms','aiLayouts','aiPresets','partyIds','selectedQuestId','inventory','guild','flags','quest_progress','quest_resources','adventure']);
  const CHARACTER_KEYS=new Set(['id','name','level','job','base_hp','base_mp','stats','skills','equippedSkillId','passiveIds','skillLoadoutIds','formalAiBinding','equipment','weaponStyle','jobHistory','growthHistory','createdAt','formation_position']);
  const CHARACTER_REQUIRED=Object.freeze(['id','name','level','job','base_hp','base_mp','stats','skills','equippedSkillId','formalAiBinding','equipment','weaponStyle','jobHistory','growthHistory','createdAt','formation_position']);
  const PROGRAM_KEYS=new Set(['schema_version','data_version','id','name','version','status','entry_node_id','nodes','edges','subroutines','result_slots','tags','description','updated_at','compiled']);
  const NODE_KEYS=new Set(['instance_id','master_node_id','master_data_version','node_type','position','parameters','target_selector','target_source','comment']);
  const POSITION_KEYS=new Set(['x','y']);
  const EDGE_KEYS=new Set(['edge_id','from','transition_kind','to','subroutine_id','return_to']);
  const ENDPOINT_KEYS=new Set(['node_id','port_id']);
  const SUBROUTINE_KEYS=new Set(['id','entry_node_id']);
  const RESULT_SLOT_KEYS=new Set(['slot_id','name','value_type']);
  const TARGET_SOURCE_KEYS=new Set(['kind','result_slot_id']);
  const LAYOUT_KEYS=new Set(['schema_version','data_version','layout_id','program_id','width','height','chips','extensions']);
  const CHIP_KEYS=new Set(['instance_id','x','y','rotation']);
  const EXTENSION_KEYS=new Set(['id','x','y','shape','rotation']);
  const PRESET_KEYS=new Set(['preset_id','name','source','program','layout','created_at','updated_at']);
  const PRESET_REQUIRED=Object.freeze([...PRESET_KEYS]);
  const clone=value=>value==null?value:JSON.parse(JSON.stringify(value));
  const isObject=value=>value&&typeof value==='object'&&!Array.isArray(value);
  const own=(value,key)=>Object.prototype.hasOwnProperty.call(value,key);
  function schemaError(errors,at,message){errors.push(`${at}: ${message}`);}
  function allowedKeys(errors,value,allowed,at){if(!isObject(value)){schemaError(errors,at,'object required');return false;}for(const key of Object.keys(value))if(!allowed.has(key))schemaError(errors,`${at}.${key}`,'field is not allowed');return true;}
  function requiredKeys(errors,value,required,at){for(const key of required)if(!own(value,key))schemaError(errors,`${at}.${key}`,'field is required');}
  function nonEmptyString(value){return typeof value==='string'&&value.trim().length>0;}
  function normalizeBinding(value){
    if(value==null)return null;if(!isObject(value))return null;const keys=Object.keys(value).sort();if(keys.length!==2||keys[0]!=='layout_id'||keys[1]!=='program_id')return null;
    const programId=String(value.program_id||'').trim(),layoutId=String(value.layout_id||'').trim();return programId&&layoutId?{program_id:programId,layout_id:layoutId}:null;
  }
  function validateProgramShape(program,at){
    const errors=[];if(!allowedKeys(errors,program,PROGRAM_KEYS,at))return errors;
    requiredKeys(errors,program,['schema_version','data_version','id','name','version','status','entry_node_id','nodes','edges','subroutines'],at);
    if(program.schema_version!==Program.SCHEMA_VERSION)schemaError(errors,`${at}.schema_version`,`must be ${Program.SCHEMA_VERSION}`);
    if(!nonEmptyString(program.data_version))schemaError(errors,`${at}.data_version`,'non-empty string required');if(!nonEmptyString(program.id))schemaError(errors,`${at}.id`,'non-empty string required');if(!nonEmptyString(program.name))schemaError(errors,`${at}.name`,'non-empty string required');
    if(!Number.isInteger(program.version)||program.version<1)schemaError(errors,`${at}.version`,'positive integer required');if(!['draft','valid','invalid','archived'].includes(program.status))schemaError(errors,`${at}.status`,'invalid value');if(typeof program.entry_node_id!=='string')schemaError(errors,`${at}.entry_node_id`,'string required');
    if(!Array.isArray(program.nodes))schemaError(errors,`${at}.nodes`,'array required');if(!Array.isArray(program.edges))schemaError(errors,`${at}.edges`,'array required');if(!Array.isArray(program.subroutines))schemaError(errors,`${at}.subroutines`,'array required');
    if(own(program,'result_slots')){if(!Array.isArray(program.result_slots))schemaError(errors,`${at}.result_slots`,'array required');else{const seenSlots=new Set();for(const [index,slot] of program.result_slots.entries()){const sat=`${at}.result_slots[${index}]`;if(!allowedKeys(errors,slot,RESULT_SLOT_KEYS,sat))continue;requiredKeys(errors,slot,['slot_id','name','value_type'],sat);if(!nonEmptyString(slot.slot_id)||!/^ARS-[A-Za-z0-9_.-]+$/.test(slot.slot_id))schemaError(errors,`${sat}.slot_id`,'ARS identifier required');else if(seenSlots.has(slot.slot_id))schemaError(errors,`${sat}.slot_id`,'duplicate result slot');else seenSlots.add(slot.slot_id);if(!nonEmptyString(slot.name))schemaError(errors,`${sat}.name`,'non-empty string required');if(slot.value_type!=='UNIT_SET')schemaError(errors,`${sat}.value_type`,'must be UNIT_SET');}}}
    if(own(program,'tags')&&(!Array.isArray(program.tags)||program.tags.some(x=>!nonEmptyString(x))))schemaError(errors,`${at}.tags`,'array of non-empty strings required');if(own(program,'description')&&typeof program.description!=='string')schemaError(errors,`${at}.description`,'string required');if(own(program,'updated_at')&&typeof program.updated_at!=='string')schemaError(errors,`${at}.updated_at`,'string required');if(own(program,'compiled')&&program.compiled!==null&&!isObject(program.compiled))schemaError(errors,`${at}.compiled`,'object or null required');
    const nodeIds=new Set();
    for(const [index,node] of (program.nodes||[]).entries()){
      const nat=`${at}.nodes[${index}]`;if(!allowedKeys(errors,node,NODE_KEYS,nat))continue;requiredKeys(errors,node,['instance_id','master_node_id','node_type','position','parameters'],nat);
      if(!nonEmptyString(node.instance_id))schemaError(errors,`${nat}.instance_id`,'non-empty string required');else if(nodeIds.has(node.instance_id))schemaError(errors,`${nat}.instance_id`,'duplicate instance_id');else nodeIds.add(node.instance_id);
      if(!nonEmptyString(node.master_node_id))schemaError(errors,`${nat}.master_node_id`,'non-empty string required');if(!['search','condition','action'].includes(node.node_type))schemaError(errors,`${nat}.node_type`,'invalid value');
      const expectedPrefix=node.node_type==='search'?'AIS-':node.node_type==='condition'?'AIC-':node.node_type==='action'?'AIA-':'';if(expectedPrefix&&!String(node.master_node_id||'').startsWith(expectedPrefix))schemaError(errors,`${nat}.master_node_id`,`must use ${expectedPrefix} prefix`);
      if(allowedKeys(errors,node.position,POSITION_KEYS,`${nat}.position`)){requiredKeys(errors,node.position,['x','y'],`${nat}.position`);if(typeof node.position.x!=='number'||typeof node.position.y!=='number')schemaError(errors,`${nat}.position`,'numeric x/y required');}
      if(!isObject(node.parameters))schemaError(errors,`${nat}.parameters`,'object required');
      if(own(node,'target_selector')){const b=node.target_selector;if(b!==null&&(!isObject(b)||Object.keys(b).some(k=>!['selector_id','params'].includes(k))||!nonEmptyString(b.selector_id)||!/^ATS-/.test(b.selector_id)||!isObject(b.params)))schemaError(errors,`${nat}.target_selector`,'ATS structured binding or null required');if(node.node_type!=='action'&&b!==null)schemaError(errors,`${nat}.target_selector`,'only action may have target_selector');}
      if(own(node,'target_source')){const source=node.target_source;if(source!==null){if(!isObject(source)||!allowedKeys(errors,source,TARGET_SOURCE_KEYS,`${nat}.target_source`))schemaError(errors,`${nat}.target_source`,'SEARCH_RESULT binding or null required');else{requiredKeys(errors,source,['kind','result_slot_id'],`${nat}.target_source`);if(source.kind!=='SEARCH_RESULT')schemaError(errors,`${nat}.target_source.kind`,'must be SEARCH_RESULT');if(!nonEmptyString(source.result_slot_id)||!/^ARS-[A-Za-z0-9_.-]+$/.test(source.result_slot_id))schemaError(errors,`${nat}.target_source.result_slot_id`,'ARS identifier required');}if(node.node_type!=='action')schemaError(errors,`${nat}.target_source`,'only action may have target_source');}}
    }
    for(const [index,edge] of (program.edges||[]).entries()){
      const eat=`${at}.edges[${index}]`;if(!allowedKeys(errors,edge,EDGE_KEYS,eat))continue;requiredKeys(errors,edge,['edge_id','from','transition_kind'],eat);if(!nonEmptyString(edge.edge_id))schemaError(errors,`${eat}.edge_id`,'non-empty string required');
      if(allowedKeys(errors,edge.from,ENDPOINT_KEYS,`${eat}.from`)){requiredKeys(errors,edge.from,['node_id','port_id'],`${eat}.from`);if(!nonEmptyString(edge.from.node_id)||!nonEmptyString(edge.from.port_id))schemaError(errors,`${eat}.from`,'non-empty node_id/port_id required');}
      const kind=String(edge.transition_kind||'');if(!['NODE','CALL','RETURN'].includes(kind))schemaError(errors,`${eat}.transition_kind`,'NODE/CALL/RETURN required');
      if(kind==='NODE'){
        if(!own(edge,'to'))schemaError(errors,`${eat}.to`,'required for NODE');if(own(edge,'subroutine_id')||own(edge,'return_to'))schemaError(errors,eat,'NODE must not have subroutine_id/return_to');
        if(own(edge,'to')&&allowedKeys(errors,edge.to,ENDPOINT_KEYS,`${eat}.to`)){requiredKeys(errors,edge.to,['node_id','port_id'],`${eat}.to`);if(!nonEmptyString(edge.to.node_id)||!nonEmptyString(edge.to.port_id))schemaError(errors,`${eat}.to`,'non-empty node_id/port_id required');}
      }else if(kind==='CALL'){
        if(own(edge,'to'))schemaError(errors,`${eat}.to`,'forbidden for CALL');if(!nonEmptyString(edge.subroutine_id))schemaError(errors,`${eat}.subroutine_id`,'required for CALL');
        if(!own(edge,'return_to'))schemaError(errors,`${eat}.return_to`,'required for CALL');else if(allowedKeys(errors,edge.return_to,ENDPOINT_KEYS,`${eat}.return_to`)){requiredKeys(errors,edge.return_to,['node_id','port_id'],`${eat}.return_to`);if(!nonEmptyString(edge.return_to.node_id)||!nonEmptyString(edge.return_to.port_id))schemaError(errors,`${eat}.return_to`,'non-empty node_id/port_id required');}
      }else if(kind==='RETURN'){
        if(own(edge,'to')||own(edge,'subroutine_id')||own(edge,'return_to'))schemaError(errors,eat,'RETURN must not have to/subroutine_id/return_to');
      }
    }
    for(const [index,row] of (program.subroutines||[]).entries()){
      const sat=`${at}.subroutines[${index}]`;if(!allowedKeys(errors,row,SUBROUTINE_KEYS,sat))continue;requiredKeys(errors,row,['id','entry_node_id'],sat);if(!nonEmptyString(row.id)||!nonEmptyString(row.entry_node_id))schemaError(errors,sat,'non-empty id/entry_node_id required');
    }
    return errors;
  }
  function validateLayoutShape(layout,at){
    const errors=[];if(!allowedKeys(errors,layout,LAYOUT_KEYS,at))return errors;requiredKeys(errors,layout,[...LAYOUT_KEYS],at);
    for(const [index,chip] of (Array.isArray(layout.chips)?layout.chips:[]).entries()){const cat=`${at}.chips[${index}]`;if(allowedKeys(errors,chip,CHIP_KEYS,cat))requiredKeys(errors,chip,[...CHIP_KEYS],cat);}
    for(const [index,extension] of (Array.isArray(layout.extensions)?layout.extensions:[]).entries()){const eat=`${at}.extensions[${index}]`;if(allowedKeys(errors,extension,EXTENSION_KEYS,eat))requiredKeys(errors,extension,[...EXTENSION_KEYS],eat);}
    Layout.validateLayout(layout).forEach(message=>schemaError(errors,at,message));return errors;
  }
  function normalizePreset(value){
    if(!isObject(value))return null;const presetId=String(value.preset_id||'').trim(),name=String(value.name||'').trim();if(!presetId||!name||!isObject(value.program)||!isObject(value.layout))return null;
    const program=Program.normalizeProgram(value.program),layout=Layout.normalizeLayout(value.layout);if(!program.id||!layout.layout_id||layout.program_id!==program.id)return null;program.compiled=null;program.status='draft';
    return{preset_id:presetId,name,source:String(value.source||'user'),program,layout,created_at:String(value.created_at||''),updated_at:String(value.updated_at||'')};
  }
  function nextPresetId(rows){let max=0;for(const row of rows||[]){const match=PRESET_ID_PATTERN.exec(String(row?.preset_id||''));if(match)max=Math.max(max,Number(match[1]));}return `AIPR-${String(max+1).padStart(4,'0')}`;}
  function duplicateIds(rows,key){const seen=new Set(),duplicates=[];for(const row of rows||[]){const id=String(row?.[key]||'');if(!id)continue;if(seen.has(id)&&!duplicates.includes(id))duplicates.push(id);seen.add(id);}return duplicates;}
  function validatePreset(preset){
    const errors=[];if(!allowedKeys(errors,preset,PRESET_KEYS,'preset'))return errors;requiredKeys(errors,preset,PRESET_REQUIRED,'preset');if(!nonEmptyString(preset.preset_id))schemaError(errors,'preset.preset_id','non-empty string required');if(!nonEmptyString(preset.name))schemaError(errors,'preset.name','non-empty string required');if(typeof preset.source!=='string')schemaError(errors,'preset.source','string required');if(typeof preset.created_at!=='string'||typeof preset.updated_at!=='string')schemaError(errors,'preset timestamps','strings required');
    errors.push(...validateProgramShape(preset.program,'preset.program'));errors.push(...validateLayoutShape(preset.layout,'preset.layout'));if(isObject(preset.program)&&isObject(preset.layout)&&preset.program.id&&preset.layout.program_id!==preset.program.id)schemaError(errors,'preset','Program/Layout reference mismatch');return errors;
  }
  function validateCurrent(save){
    const errors=[];if(!allowedKeys(errors,save,SAVE_KEYS,'save'))return errors;requiredKeys(errors,save,SAVE_REQUIRED,'save');if(Number(save.saveVersion)!==SAVE_VERSION)schemaError(errors,'save.saveVersion',`must be ${SAVE_VERSION}`);
    if(!Array.isArray(save.characters))schemaError(errors,'save.characters','array required');if(!Array.isArray(save.aiPrograms))schemaError(errors,'save.aiPrograms','array required');if(!Array.isArray(save.aiLayouts))schemaError(errors,'save.aiLayouts','array required');if(!Array.isArray(save.aiPresets))schemaError(errors,'save.aiPresets','array required');
    for(const key of ['gameSettings','tutorialProgress'])if(own(save,key)&&!isObject(save[key]))schemaError(errors,`save.${key}`,'object required when present');
    duplicateIds(save.aiPrograms,'id').forEach(id=>schemaError(errors,'save.aiPrograms',`duplicate id ${id}`));duplicateIds(save.aiLayouts,'layout_id').forEach(id=>schemaError(errors,'save.aiLayouts',`duplicate id ${id}`));duplicateIds(save.aiPresets,'preset_id').forEach(id=>schemaError(errors,'save.aiPresets',`duplicate id ${id}`));
    for(const [index,character] of (save.characters||[]).entries()){
      const at=`save.characters[${index}]`;if(!allowedKeys(errors,character,CHARACTER_KEYS,at))continue;requiredKeys(errors,character,CHARACTER_REQUIRED,at);if(!nonEmptyString(character.id))schemaError(errors,`${at}.id`,'non-empty string required');if(!nonEmptyString(character.name))schemaError(errors,`${at}.name`,'non-empty string required');if(!Array.isArray(character.skills)||character.skills.some(id=>!nonEmptyString(id)))schemaError(errors,`${at}.skills`,'array of non-empty skill ids required');else if(new Set(character.skills).size!==character.skills.length)schemaError(errors,`${at}.skills`,'duplicate skill id is not allowed');if(typeof character.equippedSkillId!=='string')schemaError(errors,`${at}.equippedSkillId`,'string required');else if(character.equippedSkillId&&Array.isArray(character.skills)&&!character.skills.includes(character.equippedSkillId))schemaError(errors,`${at}.equippedSkillId`,'must reference owned skill id');for(const key of ['passiveIds','skillLoadoutIds'])if(own(character,key)){const ids=character[key];if(!Array.isArray(ids)||ids.some(id=>!nonEmptyString(id)))schemaError(errors,`${at}.${key}`,'array of non-empty ids required');else if(new Set(ids).size!==ids.length)schemaError(errors,`${at}.${key}`,'duplicate id is not allowed');}if(character.weaponStyle!=null&&!['single','dual_wield','two_hand','weapon_shield'].includes(String(character.weaponStyle)))schemaError(errors,`${at}.weaponStyle`,'single / dual_wield / two_hand / weapon_shield required');if(character.formalAiBinding!=null&&!normalizeBinding(character.formalAiBinding))schemaError(errors,`${at}.formalAiBinding`,'invalid binding');
    }
    for(const [index,program] of (save.aiPrograms||[]).entries()){
      const at=`save.aiPrograms[${index}]`; errors.push(...validateProgramShape(program,at)); const match=PLAYER_PROGRAM_ID_PATTERN.exec(String(program?.id||'')); if(!match||Number(match[1])%2!==0)schemaError(errors,`${at}.id`,'Player-authored Program ID must use the even AIP numeric namespace');
    }
    for(const [index,layout] of (save.aiLayouts||[]).entries()){
      const at=`save.aiLayouts[${index}]`; errors.push(...validateLayoutShape(layout,at)); const layoutMatch=PLAYER_LAYOUT_ID_PATTERN.exec(String(layout?.layout_id||'')),programMatch=PLAYER_PROGRAM_ID_PATTERN.exec(String(layout?.program_id||'')); if(!layoutMatch||Number(layoutMatch[1])%2!==0)schemaError(errors,`${at}.layout_id`,'Player-authored Layout ID must use the even AIL numeric namespace'); if(!programMatch||Number(programMatch[1])%2!==0)schemaError(errors,`${at}.program_id`,'Player-authored Layout program_id must use the even AIP numeric namespace');
    }
    for(const [index,preset] of (save.aiPresets||[]).entries())validatePreset(preset).forEach(message=>errors.push(`save.aiPresets[${index}]: ${message}`));
    return errors;
  }
  function assertCurrent(raw){
    if(!isObject(raw))throw new Error('Save Dataが不正です。');const source=clone(raw),errors=validateCurrent(source);if(errors.length){const error=new Error(`Save Data Version ${SAVE_VERSION}が不正です。\n${errors.join('\n')}`);error.code='FORMAL_AI_SAVE_CURRENT_INVALID';error.errors=errors;throw error;}
    source.aiPrograms=source.aiPrograms.map(row=>Program.normalizeProgram(row));source.aiLayouts=source.aiLayouts.map(row=>Layout.normalizeLayout(row));source.aiPresets=source.aiPresets.map(normalizePreset);for(const character of source.characters)character.formalAiBinding=normalizeBinding(character.formalAiBinding);return source;
  }
  function loadForCharacter(saveValue,characterId){const save=isObject(saveValue)?saveValue:{};const character=(save.characters||[]).find(row=>String(row?.id||'')===String(characterId||'')),binding=normalizeBinding(character?.formalAiBinding);if(!binding)return null;const program=(save.aiPrograms||[]).find(row=>String(row?.id||'')===binding.program_id),layout=(save.aiLayouts||[]).find(row=>String(row?.layout_id||'')===binding.layout_id);if(!program||!layout||String(layout.program_id||'')!==String(program.id||''))return null;return{binding:clone(binding),program:Program.normalizeProgram(program),layout:Layout.normalizeLayout(layout)};}
  function runtimeMatchesProgram(runtime,program,dataVersion){return !!runtime&&isObject(runtime)&&runtime.schema_version===Program.SCHEMA_VERSION&&String(runtime.data_version||'')===String(dataVersion||program?.data_version||'')&&String(runtime.program_id||'')===String(program?.id||'')&&Number(runtime.program_version)===Number(program?.version);}
  function runtimeForCharacter(saveValue,characterId){const loaded=loadForCharacter(saveValue,characterId),runtime=loaded?.program?.compiled;if(!loaded||!runtimeMatchesProgram(runtime,loaded.program,loaded.program.data_version))return null;return clone(runtime);}
  function developerRuntimeForBinding(catalog,bindingValue){const binding=normalizeBinding(bindingValue);if(!binding||!isObject(catalog))return null;const programs=Array.isArray(catalog.developer_programs)?catalog.developer_programs:[],layouts=Array.isArray(catalog.developer_program_layouts)?catalog.developer_program_layouts:[],runtimes=Array.isArray(catalog.developer_program_runtime)?catalog.developer_program_runtime:[];const program=programs.find(row=>String(row?.id||'')===binding.program_id),layout=layouts.find(row=>String(row?.layout_id||'')===binding.layout_id);if(!program||!layout||String(layout.program_id||'')!==binding.program_id)return null;const runtime=runtimes.find(row=>String(row?.program_id||'')===binding.program_id&&Number(row?.program_version)===Number(program.version));if(!runtimeMatchesProgram(runtime,program,catalog.data_version||program.data_version))return null;return{binding:clone(binding),program:clone(program),layout:clone(layout),runtime:clone(runtime)};}
  function playerProgramForBinding(saveValue,bindingValue){const save=isObject(saveValue)?saveValue:{},binding=normalizeBinding(bindingValue);if(!binding)return null;const program=(save.aiPrograms||[]).find(row=>String(row?.id||'')===binding.program_id),layout=(save.aiLayouts||[]).find(row=>String(row?.layout_id||'')===binding.layout_id);if(!program||!layout||String(layout.program_id||'')!==binding.program_id)return null;const normalizedProgram=Program.normalizeProgram(program),normalizedLayout=Layout.normalizeLayout(layout),runtime=normalizedProgram.compiled;return{binding:clone(binding),program:normalizedProgram,layout:normalizedLayout,runtime:runtimeMatchesProgram(runtime,normalizedProgram,normalizedProgram.data_version)?clone(runtime):null};}
  function bindingAuthority(saveValue,catalog,bindingValue){const binding=normalizeBinding(bindingValue);if(!binding)return{status:'unbound',binding:null,source:null};const player=playerProgramForBinding(saveValue,binding),developer=developerRuntimeForBinding(catalog,binding);if(player&&developer)return{status:'ambiguous',binding:clone(binding),source:null,player:clone(player),developer:clone(developer)};if(player)return{status:'resolved',binding:clone(binding),source:'player',...player};if(developer)return{status:'resolved',binding:clone(binding),source:'developer',...developer};return{status:'unresolved',binding:clone(binding),source:null};}
  function characterBindingAuthority(saveValue,catalog,characterId){const save=isObject(saveValue)?saveValue:{},character=(save.characters||[]).find(row=>String(row?.id||'')===String(characterId||''));return bindingAuthority(save,catalog,character?.formalAiBinding);}
  function equipCharacterBinding(saveValue,characterId,bindingValue,catalog){const next=assertCurrent(saveValue),character=next.characters.find(row=>String(row?.id||'')===String(characterId||''));if(!character)throw new Error('AI装着対象キャラクターが見つかりません。');const binding=normalizeBinding(bindingValue);if(!binding)throw new Error('Formal AI bindingが不正です。');const authority=bindingAuthority(next,catalog,binding);if(authority.status==='ambiguous'){const error=new Error('Player/Developer AI Program IDが衝突しているため装着できません。');error.code='FORMAL_AI_BINDING_AMBIGUOUS';throw error;}if(authority.status!=='resolved'){const error=new Error('装着対象Formal AI ProgramをCurrent authorityで解決できません。');error.code='FORMAL_AI_BINDING_UNRESOLVED';throw error;}character.formalAiBinding=clone(binding);return{save:next,binding:clone(binding),source:authority.source};}
  function clearCharacterBinding(saveValue,characterId){const next=assertCurrent(saveValue),character=next.characters.find(row=>String(row?.id||'')===String(characterId||''));if(!character)throw new Error('AI解除対象キャラクターが見つかりません。');character.formalAiBinding=null;return{save:next,binding:null};}
  function resolveRuntimeForActor({save,catalog,actor}={}){if(!isObject(actor))return null;if(nonEmptyString(actor.characterId)){const authority=characterBindingAuthority(save,catalog,actor.characterId);if(authority.status!=='resolved'||!authority.runtime)return null;return{owner_kind:'character',program_source:authority.source,binding:clone(authority.binding),program:clone(authority.program),layout:clone(authority.layout),runtime:clone(authority.runtime)};}if(nonEmptyString(actor.monsterId)){const binding=normalizeBinding(actor.formalAiBinding);if(!binding)return null;const loaded=developerRuntimeForBinding(catalog,binding);return loaded?{owner_kind:'monster',program_source:'developer',...loaded}:null;}return null;}
  function runtimeForActor(input){return resolveRuntimeForActor(input)?.runtime||null;}
  function resolvedBinding(bindingValue,masterSnapshotId){const binding=normalizeBinding(bindingValue),snapshot=String(masterSnapshotId||'').trim();if(!binding||!snapshot)return null;return{program_id:binding.program_id,layout_id:binding.layout_id,master_snapshot_id:snapshot};}
  function upsert(rows,key,value){const id=String(value?.[key]||''),index=rows.findIndex(row=>String(row?.[key]||'')===id);if(index>=0)rows[index]=clone(value);else rows.push(clone(value));}
  function nextOwnedNumericId(rows,key,pattern,prefix,parity){let max=0;for(const row of Array.isArray(rows)?rows:[]){const match=pattern.exec(String(row?.[key]||''));if(match)max=Math.max(max,Number(match[1]));}let number=max+1;if(number%2!==parity)number+=1;return `${prefix}-${String(number).padStart(4,'0')}`;}
  function nextPlayerProgramId(saveValue,catalog){const rows=[...(Array.isArray(saveValue?.aiPrograms)?saveValue.aiPrograms:[]),...(Array.isArray(catalog?.developer_programs)?catalog.developer_programs:[])];return nextOwnedNumericId(rows,'id',PLAYER_PROGRAM_ID_PATTERN,'AIP',0);}
  function nextPlayerLayoutId(saveValue,catalog){const rows=[...(Array.isArray(saveValue?.aiLayouts)?saveValue.aiLayouts:[]),...(Array.isArray(catalog?.developer_program_layouts)?catalog.developer_program_layouts:[])];return nextOwnedNumericId(rows,'layout_id',PLAYER_LAYOUT_ID_PATTERN,'AIL',0);}
  function playerBindingRows(saveValue){const save=isObject(saveValue)?saveValue:{};return(Array.isArray(save.aiPrograms)?save.aiPrograms:[]).map(program=>{const layout=(save.aiLayouts||[]).find(row=>String(row?.program_id||'')===String(program?.id||''));return layout?{source:'player',binding:{program_id:String(program.id),layout_id:String(layout.layout_id)},program:clone(program),layout:clone(layout)}:null;}).filter(Boolean);}
  function developerBindingRows(catalog){return(Array.isArray(catalog?.developer_programs)?catalog.developer_programs:[]).map(program=>{const layout=(catalog.developer_program_layouts||[]).find(row=>String(row?.program_id||'')===String(program?.id||''));const runtime=(catalog.developer_program_runtime||[]).find(row=>String(row?.program_id||'')===String(program?.id||'')&&Number(row?.program_version)===Number(program?.version));return layout&&runtimeMatchesProgram(runtime,program,catalog?.data_version||program?.data_version)?{source:'developer',binding:{program_id:String(program.id),layout_id:String(layout.layout_id)},program:clone(program),layout:clone(layout),runtime:clone(runtime)}:null;}).filter(Boolean);}
  function availableCharacterBindings(saveValue,catalog){const rows=[...playerBindingRows(saveValue),...developerBindingRows(catalog)],seen=new Set(),out=[];for(const row of rows){const key=`${row.binding.program_id}|${row.binding.layout_id}`;if(seen.has(key)){out.push({...row,ambiguous:true});continue;}seen.add(key);out.push({...row,ambiguous:false});}return clone(out);}
  function saveForCharacter(saveValue,characterId,programValue,layoutValue,options){
    const next=assertCurrent(saveValue),character=next.characters.find(row=>String(row?.id||'')===String(characterId||''));if(!character)throw new Error('AI保存対象キャラクターが見つかりません。');const current=loadForCharacter(next,characterId),now=String(options?.now||new Date().toISOString()),program=Program.normalizeProgram(programValue),layout=Layout.normalizeLayout(layoutValue);program.compiled=null;
    const programId=current?.binding?.program_id||nextPlayerProgramId(next,options?.catalog),layoutId=current?.binding?.layout_id||nextPlayerLayoutId(next,options?.catalog);if((options?.catalog?.developer_programs||[]).some(row=>String(row?.id||'')===programId)||(options?.catalog?.developer_program_layouts||[]).some(row=>String(row?.layout_id||'')===layoutId)){const error=new Error('Developer-authored AI Program/LayoutとIDが衝突しているためPlayer Programを保存できません。');error.code='FORMAL_AI_PROGRAM_ID_COLLISION';throw error;}program.id=programId;program.name=String(program.name||`${character.name||'冒険者'} AI`);program.status='valid';program.updated_at=now;const previous=next.aiPrograms.find(row=>String(row?.id||'')===programId);program.version=previous?Math.max(1,Number(previous.version)||1)+1:Math.max(1,Number(program.version)||1);layout.schema_version=Layout.SCHEMA_VERSION;layout.data_version=program.data_version;layout.layout_id=layoutId;layout.program_id=programId;
    const layoutErrors=validateLayoutShape(layout,'layout');if(layoutErrors.length){const error=new Error(`AI Layoutを保存できません。\n${layoutErrors.join('\n')}`);error.code='FORMAL_AI_LAYOUT_INVALID';error.errors=layoutErrors;throw error;}upsert(next.aiPrograms,'id',program);upsert(next.aiLayouts,'layout_id',layout);character.formalAiBinding={program_id:programId,layout_id:layoutId};const errors=validateCurrent(next);if(errors.length){const error=new Error(`AI保存後のSave Dataが不正です。\n${errors.join('\n')}`);error.code='FORMAL_AI_SAVE_ATOMIC_VALIDATION_FAILED';error.errors=errors;throw error;}return{save:next,binding:clone(character.formalAiBinding),program:clone(program),layout:clone(layout)};
  }
  function userPresets(saveValue){const save=assertCurrent(saveValue);return clone(save.aiPresets);}
  function createUserPreset(saveValue,name,programValue,layoutValue,options){const next=assertCurrent(saveValue),now=String(options?.now||new Date().toISOString()),presetId=nextPresetId(next.aiPresets),program=Program.normalizeProgram(programValue),layout=Layout.normalizeLayout(layoutValue);program.compiled=null;program.status='draft';if(layout.program_id!==program.id)layout.program_id=program.id;const preset=normalizePreset({preset_id:presetId,name:String(name||'').trim(),source:'user',program,layout,created_at:now,updated_at:now}),errors=validatePreset(preset);if(errors.length){const error=new Error(`Presetを保存できません。\n${errors.join('\n')}`);error.code='FORMAL_AI_PRESET_INVALID';error.errors=errors;throw error;}next.aiPresets.push(preset);return{save:next,preset:clone(preset)};}
  function renameUserPreset(saveValue,presetId,name,options){const next=assertCurrent(saveValue),preset=next.aiPresets.find(row=>row.preset_id===String(presetId||''));if(!preset)throw new Error('Presetが見つかりません。');const nextName=String(name||'').trim();if(!nextName)throw new Error('Preset名を入力してください。');preset.name=nextName;preset.updated_at=String(options?.now||new Date().toISOString());return{save:next,preset:clone(preset)};}
  function duplicateUserPreset(saveValue,presetId,name,options){const next=assertCurrent(saveValue),source=next.aiPresets.find(row=>row.preset_id===String(presetId||''));if(!source)throw new Error('Presetが見つかりません。');const now=String(options?.now||new Date().toISOString()),copy=clone(source);copy.preset_id=nextPresetId(next.aiPresets);copy.name=String(name||`${source.name} のコピー`).trim();copy.created_at=now;copy.updated_at=now;next.aiPresets.push(copy);return{save:next,preset:clone(copy)};}
  function deleteUserPreset(saveValue,presetId){const next=assertCurrent(saveValue),before=next.aiPresets.length;next.aiPresets=next.aiPresets.filter(row=>row.preset_id!==String(presetId||''));if(next.aiPresets.length===before)throw new Error('Presetが見つかりません。');return{save:next,deleted_preset_id:String(presetId||'')};}
  return Object.freeze({SAVE_VERSION,normalizeBinding,normalizePreset,nextPresetId,validatePreset,validateCurrent,assertCurrent,loadForCharacter,runtimeForCharacter,playerProgramForBinding,developerRuntimeForBinding,bindingAuthority,characterBindingAuthority,equipCharacterBinding,clearCharacterBinding,resolveRuntimeForActor,runtimeForActor,resolvedBinding,nextPlayerProgramId,nextPlayerLayoutId,playerBindingRows,developerBindingRows,availableCharacterBindings,saveForCharacter,userPresets,createUserPreset,renameUserPreset,duplicateUserPreset,deleteUserPreset});
});
